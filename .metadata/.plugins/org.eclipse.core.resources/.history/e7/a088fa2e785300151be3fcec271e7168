package com.parsch.Utilities;

/*
 * File:	OConsole.java
 * Package:	com.parsch.Utilities
 * Purpose:	To handle all the static
 * 			functions that fall under
 * 			console output.
 */

/**
 * Holds several functions for easy access to output through the console. 
 * @see #print(String toDisplay)
 * @see #println(String toDisplay)
 * @see #print(String toDisplay, Boolean addNewLine)
 * @author Anthony Parsch Jr.
 */
public class OConsole
{
	/**
	 * Print a string to the console without a new line being added to the end of the string.
	 * @param toDisplay - The string to be displayed.
	 */
	public static void print(String toDisplay)
	{
		print(toDisplay, false);
	}
	
	/**
	 * Print a string to the console with a new line being added at the end of the string.
	 * @param toDisplay - The string to be displayed.
	 */
	public static void println(String toDisplay)
	{
		print(toDisplay, true);
	}
	
	/**
	 * Print a string to the console, with the ability to determine if it is followed by a new line.
	 * @param toDisplay - The string to be displayed.
	 * @param addNewLine - Whether, or not, to add a new line on the end of the string.
	 */
	public static void print(String toDisplay, Boolean addNewLine)
	{
		if (addNewLine)
		{
			System.out.println(toDisplay);
		}
		else
		{
			System.out.print(toDisplay);
		}
	}
}
